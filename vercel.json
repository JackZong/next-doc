{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "pnpm db:generate && pnpm db:push --force && next build",
  "installCommand": "pnpm install",
  "regions": ["hkg1"],
  "env": {
    "DB_TYPE": "postgres"
  }
}
